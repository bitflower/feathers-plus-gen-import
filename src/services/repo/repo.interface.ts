
// Define TypeScript interface for service `repo`. (Can be re-generated.)
// !code: imports // !end
// !code: init // !end

// tslint:disable-next-line:no-empty-interface
export interface CoreposBase {
  // !code: interface // !end
}

// tslint:disable-next-line:no-empty-interface
export interface Corepos extends CoreposBase {
  // !code: more // !end
}

// !code: funcs // !end
// !code: end // !end
